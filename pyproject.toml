[project]
name = "problem-kcn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Zellsed", email = "zellsed123@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
problem-kcn = "problem_kcn:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
